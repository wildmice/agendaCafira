CREATE TABLE `cafira`.`agenda` (
  `idagenda` INT NOT NULL AUTO_INCREMENT,
  `Nombre` VARCHAR(70) NOT NULL,
  `Telefono` VARCHAR(45) NOT NULL,
  `Fecha Nac` DATE NOT NULL,
  PRIMARY KEY (`idagenda`));